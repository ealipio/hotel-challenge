import React from 'react';

interface Props {
  name: string;
  ownerLogin: string;
  stargazersCount: number;
  description: string;
}

/**
 *
 * Fill in the react component below with following:
 *    - Use the name as the card header
 *    - Have a bulleted list that contains ownerLogin, stargazersCount, and description
 *    - Be centered vertically in the middle of the page
 *
 */

export const Card = ({
  name,
  ownerLogin,
  stargazersCount,
  description
}: Props) => {
  return null;
};
